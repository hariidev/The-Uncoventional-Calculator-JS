const currentResult = 0;

let numValue = 10;



let defauleValue = currentResult;

defauleValue = defauleValue + 10 * 8 / 2 ;

let calculateDescription = `${defauleValue}=10 * 8 / 2 `; 

outputResults(defauleValue, calculateDescription);
 